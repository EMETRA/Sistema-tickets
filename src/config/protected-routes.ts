export const protectedRoutes = [
    "/home",
    "/tickets",
    "/tickets-creation",
    "/mis-tickets",
    "/statistics",
    "/equipo",
    "/config",
    "/usuarios"
];

export const publicRoutes = ["/"];

export const nologRoutes = ["/login", "/forgot-password"];