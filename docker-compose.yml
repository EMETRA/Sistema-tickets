services:
  sistema-tickets-web:
    container_name: sistema-tickets-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: "no"
    ports:
      - "${APP_PORT:-3000}:${APP_PORT:-3000}"
    env_file:
      - .env
